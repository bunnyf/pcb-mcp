# KiCad MCP Server - Requirements
#
# Runtime Dependencies:
# - Python 3.10+ (standard library only, no pip packages required)
# - KiCad 9.x with kicad-cli
# - Optional: pcbnew Python API (for zone filling, board info, auto-routing)
# - Optional: Java 17+ and FreeRouting (for auto-routing)
# - Optional: xvfb (for headless 3D rendering)
#
# This file lists development dependencies only.

# Development dependencies
pytest>=7.0
pytest-cov>=4.0
mypy>=1.0
ruff>=0.1.0
